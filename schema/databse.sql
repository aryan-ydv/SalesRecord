Create Table csv(
    region  varchar(150),
     country varchar(150),
     itemType varchar(30),
     salesChannel varchar(8),
     orderPriority  char(1),
     orderDate DATE,
     orderId double precision,
     shipDate DATE,
     unitsSold INTEGER,
     unitPrice real,
     unitCost real,
     totalRevenue real,
     totalCost real,
     totalProfit real
    
    );